# ผลตรวจ SEC1 ไก่จิก v3

ตรวจวันที่ 11 กันยายน 2026 ใช้ Node.js 24.19.0, Playwright 1.62.1 และ Chromium 152 แบบ headless บริการ LINE, Apps Script, Sheets, Drive และเวลาทดสอบเป็นระบบจำลอง ไม่ได้เข้าบัญชีหรือส่งข้อความจริง

## ผลที่ผ่าน

### Backend: 16 กลุ่มทดสอบ

1. เพิ่มโครงสร้างเกมโดยรักษาข้อมูลเงินเดิม รัน setup ซ้ำไม่เพิ่มฤดูกาล/รีเซ็ตเวลาเริ่ม
2. Token cache แบบ SHA256 สูงสุด 900 วินาที ปฏิเสธ channel ไม่ตรงและสมาชิกเรียก API Admin
3. ข้อความทั่วไปและคำสั่งเดิม 14 รูปแบบ × 4 ผู้ส่งไม่ตอบเมนู; เมนู/sec1 และช่องว่างหัวท้ายตอบตามกติกา
4. คะแนนก่อนกำหนด/ตรงวัน/เกินกำหนดตามวันไทย ปฏิเสธวันที่ไม่มีจริงและเวลาในอนาคต
5. อนุมัติเงินแล้วให้คะแนนครั้งเดียว การอนุมัติซ้ำและซ่อมคะแนนไม่ลงซ้ำ
6. แบ่งจ่ายให้คะแนนเมื่อครบ และหลักฐานมีเวลาธนาคารยืนยัน
7. หลักฐานที่ไม่มีเวลาโอนรอคะแนนจน Admin ระบุ เวลาเดิมที่ยืนยันแล้วไม่เปลี่ยนโดย API
8. จำลอง GameLedger เขียนล้มเหลว: เงินยังอนุมัติ และซ่อมคะแนนต่อได้
9. ฟักไข่หักคะแนนพร้อมรางวัลในแถวเดียว คำขอซ้ำไม่หักซ้ำ ไม่แตะ Transactions ปฏิเสธเลือกไก่ของคนอื่น
10. จำลองเขียนสำเร็จแต่ตอบกลับล้มเหลว: ใช้ request ID เดิมกู้ผลได้ และคะแนนไม่พอไม่ฟัก
11. ข้ามเดือนแล้วยังคงคอลเลกชัน/กู้ผลเดิมได้ ไม่เปิดคำขอใหม่ในเดือนเก่า
12. สมาชิกแก้ฤดูกาลไม่ได้ เดือนที่เริ่มมีผู้ฟักแล้วเปลี่ยนกติกาไม่ได้
13. แจ้งเตือนกลุ่มและส่วนตัวเฉพาะผู้ค้าง แนบเมนูหนึ่งชุดทุก push และข้ามรายการที่ LINE รับแล้วในวันเดียวกัน
14. ถอนเงินหักยอดเงินแต่ไม่หักคะแนน เปิดการเงิน/เกม/หน้า Admin ได้ครบชุดที่ทดสอบ
15. สร้างรายเดือนครั้งเดียวต่อเดือน ช่วงวันที่ 23–28 กิจกรรมยังตรวจนอกช่วงรายเดือนได้
16. ทดสอบผลซีเคร็ต/ปกติด้วยค่าการสุ่มจำลอง และการได้ซ้ำเพิ่มจำนวนสะสม

บันทึก stdout อยู่ใน tests/backend-results.txt ตัว mock ไม่จำลองความขนานของบริการ Google จริง; โค้ด mutations ใช้ Script Lock และทดสอบการ retry/การสะดุดก่อนและหลังเขียน

### Browser: ผ่านทุกสถานการณ์ใน tests/browser.test.cjs

- index.html แสดงทางเข้า LINE โดยไม่บังคับ login ทันที
- ตั้งชื่อไก่ บันทึก เปิดเมนู ฟักแบบจำลองเน็ตหลุด รีโหลดแล้วกู้ผล จิก 5 ครั้ง และเลือกไก่
- เปิดบิลของฉัน บิลรวม สรุป รายจ่าย ประวัติ คะแนน
- เปิดบิลที่เลือก ย่อรูป อัปโหลดครั้งเดียว ตรวจ draft แล้วส่งให้ Admin
- Admin ตรวจเลขอ้างอิง/เวลาโอนจริง อนุมัติสลิป และคะแนนอัปเดต
- สร้างกิจกรรม สร้างรายเดือน หยุดแผน บันทึกถอนพร้อมรูป
- ตั้งฤดูกาลอนาคต ซ่อมคะแนน และเปิดสถานะแจ้งเตือน
- มือถือ 390×844 ไม่มี overflow แนวนอนทั้งหน้า เมนูเลื่อนแนวนอนได้ตามออกแบบ
- reduced-motion หยุดภาพเคลื่อนไหว CSS
- ไม่พบ uncaught page errors และ API error ที่ไม่คาดหมายในชุดทดสอบ

แก้ระหว่างทดสอบ: URL ไม่ตามเมนูหลังเปลี่ยนหน้า, ชื่อฟังก์ชัน history ชนกับ browser History API, สีเก่าทับธีมใหม่ และฟอนต์ไทยไม่อยู่ในสภาพแวดล้อมทดสอบ ปัจจุบันใช้ฟอนต์ไทยในเว็บไซต์เอง

### ภาพและไฟล์

- Code.gs/app.js ผ่าน syntax check
- ภาพ SVG 49 ไฟล์ (ไก่ 48 + ไข่ 1)
- APNG 144 ไฟล์ 256×256 ตรวจเปิดถึงเฟรมสุดท้ายได้ มีอย่างน้อย 2 เฟรม และไม่เกิน 300,000 bytes ทุกไฟล์
- ตรวจภาพหน้า desktop/mobile/hatch จาก screenshot แล้ว
- ไม่มี Tailwind CDN; JavaScript/CSS แยกไฟล์และใช้ font-display: swap

## ขอบเขตที่ยังต้องทดสอบหลัง Deploy

ไม่ได้ทดสอบ credentials/permissions/quotas จริง, LINE Webhook/signature gateway, LINE LIFF SDK บนอุปกรณ์จริง, การเล่น APNG ใน LINE, การรับข้อความส่วนตัว, ความแม่นยำ Gemini OCR, หรือเวลา Trigger ของ Google จริง จึงไม่ใช่การรับประกันว่าปราศจากข้อผิดพลาดทุกกรณี

ไม่มี API ยืนยันธนาคาร/K PLUS ที่เชื่อมจริงในชุดนี้ การยืนยันเงินต้องทำโดย Admin ไม่ให้คะแนนจาก OCR เพียงอย่างเดียว ไม่มีการแก้ยอดเงินหรือข้อความในบัญชีของผู้ใช้ระหว่างทดสอบ

ผลทดสอบเวลาตอบสนองบริการจำลองใช้แทนความเร็ว Apps Script จริงไม่ได้ ประสิทธิภาพจริงขึ้นกับขนาด Sheets, quota และเครือข่าย ระบบอ่านข้อมูลเงินใหม่เสมอจึงไม่เพิ่ม cache ข้อมูลการเงินเพื่อเร่งความเร็ว

วิธีทดสอบซ้ำและรายการตรวจหลังติดตั้งอยู่ใน SETUP.md
